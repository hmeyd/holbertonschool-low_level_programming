#include <stdio.h>
/**
*main -Entry point
*Return: Always 0 (Success)
*/
int main(void)
{
char i;
for (i = 'z'; i Écrivez un programme qui imprime toutes les combinaisons possibles de nombres à un chiffre.
{
putchar(i);
}
putchar('\n');
return (0);
}
