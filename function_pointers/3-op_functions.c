#include <stdio.h>
#include <stdlib.h>
/**
 * op_add - Retourne la somme de deux entiers
 * @a: Premier entier
 * @b: Deuxième entier
 *
 * Return: La somme de a et b
 */
int op_add(int a, int b)
{
	return (a + b);
}
/**
 * op_sub - returns the sum
 * @a: un entier
 * @b: un entier
 * Return:la soustraction
 */
int op_sub(int a, int b)
{
	return (a - b);
}
/**
 * op_mul - returns the sum
 * @a: un entier
 * @b: un entier
 * Return: multiplication
 */
int op_mul(int a, int b)
{
	return (a * b);
}
/**
 * op_div - returns the sum
 * @a: un entier
 * @b: un entier
 * Return: division
 */
int op_div(int a, int b)
{
		return (a / b);
}
/**
 * op_mod - returns the sum
 * @a: un entier
 * @b: un entier
 * Return: modilo
 */
int op_mod(int a, int b)
{
		return (a % b);
}
