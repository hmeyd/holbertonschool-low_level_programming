#include "main.h"
/**
 * mul - multiplication
 *@a: premier numbre
 *@b: deuxieme nombre
 * Return: return le resultat de multiplication.
 */
int mul(int a, int b)
{
int mul;

mul = a * b;
return (mul);
}

