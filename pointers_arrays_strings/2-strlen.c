#include "main.h"
#include <string.h>
/**
 * _strlen - calculer la longeur d'un mot
 *
 * @s: length pointer
 * Return: la longeur de string
 */
int _strlen(char *s)
{
char a;
a = strlen(s);
return (a);
}
